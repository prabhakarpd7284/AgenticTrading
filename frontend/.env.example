VITE_API_URL=http://localhost:8000/api/v1
# Legacy bridge moved into the v2 process on port 8000 in Phase 6
# (used to live on a separate :8001). Same hostname as VITE_API_URL —
# the only difference is the /legacy/ path prefix.
VITE_LEGACY_API_URL=http://localhost:8000/api/v1
VITE_WS_URL=ws://localhost:8000
